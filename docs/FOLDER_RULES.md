# 폴더 구조 규칙 (옵션 D')

> **적용일:** 2026-02-02
> **버전:** 2.0
> **목적:** 상태 기반 폴더 관리 체계화

---

## 핵심 원칙

```
1. 실제 폴더는 ID 기반 고정 (이동 없음)
2. 🔒_views 폴더는 상태별 바로가기 (열람 전용)
3. 폴더명에서 상태 접미사 제거
4. 상태는 content_map.json에서 관리
```

---

## 폴더 구조

```
content/images/
├── 000_cover/                    # 커버 이미지 전용
├── 001_pumpkin/                  # 콘텐츠 폴더 (상태 접미사 없음)
├── 002_carrot/
├── ...
├── 140_shrimp_새우/
│
└── 🔒_views/                     # 상태별 바로가기 (심볼릭 링크)
    ├── published/                # 게시 완료
    │   ├── 001_pumpkin -> ../../001_pumpkin
    │   ├── 002_carrot -> ../../002_carrot
    │   └── ...
    ├── ready/                    # 게시 대기
    │   ├── 024_beef_소고기 -> ../../024_beef_소고기
    │   └── ...
    ├── in_progress/              # 제작 중
    └── blocked/                  # 차단됨
        ├── 056_onion_양파 -> ../../056_onion_양파
        └── ...
```

---

## 🔒_views 폴더 사용 규칙

### 절대 금지 사항

```
❌ 파일 수정 금지
❌ 파일 추가 금지
❌ 파일 삭제 금지
❌ 폴더 구조 변경 금지
```

### 허용 사항

```
✅ 오직 열람/선택만 가능
✅ 파일 복사 가능 (원본 수정 아님)
✅ 상태별 콘텐츠 확인
```

### 수정이 필요하면

원본 폴더에서 직접 작업:

```bash
# ❌ 잘못된 방법
vim 🔒_views/published/001_pumpkin/caption.txt

# ✅ 올바른 방법
vim content/images/001_pumpkin/caption.txt
```

---

## 폴더명 규칙

### 형식

```
{ID:03d}_{영문명}_{한글명}
```

### 예시

| 형식 | 예시 |
|------|------|
| 기본 | `001_pumpkin` |
| 한글 포함 | `020_pringles_프링글스` |
| 복합어 | `074_yangnyeom_chicken_양념치킨` |

### 금지

```
❌ 001_pumpkin_published     # 상태 접미사
❌ 001_pumpkin_제작완료      # 상태 접미사
❌ pumpkin                   # ID 없음
❌ 001-pumpkin               # 하이픈 사용
```

---

## 상태 동기화

### 자동 동기화

```bash
# 상태 변경 후 1회 실행
./services/scripts/sync_views.sh
```

### 동기화 시점

| 시점 | 실행 주체 |
|------|----------|
| 게시 완료 후 | 자동화 파이프라인 |
| 상태 수동 변경 후 | 수동 실행 |
| content_map 업데이트 후 | 수동 실행 |

### sync_views.sh 동작

```
1. 🔒_views 내 기존 심볼릭 링크 제거
2. content_map.json 읽기
3. 상태별 심볼릭 링크 재생성
4. 결과 출력
```

---

## content_map.json 연동

### 상태 정의

| status | 의미 | _views 위치 |
|--------|------|------------|
| published | 게시 완료 | published/ |
| ready | 게시 대기 | ready/ |
| in_progress | 제작 중 | in_progress/ |
| blocked | 차단됨 | blocked/ |

### 예시

```json
{
  "pumpkin": {
    "folder": "001_pumpkin",
    "status": "published",
    "safety": "safe"
  }
}
```

→ `🔒_views/published/001_pumpkin` 링크 생성

---

## 장점

### 1. 폴더 이동 없음

```
기존: 게시 완료 시 폴더명 변경 (_published 추가)
신규: 폴더 고정, content_map status만 변경
```

### 2. 상태별 빠른 열람

```bash
# 게시 대기 콘텐츠 확인
ls 🔒_views/ready/

# 차단된 콘텐츠 확인
ls 🔒_views/blocked/
```

### 3. 스크립트 호환성

```python
# 폴더 경로가 변경되지 않으므로 스크립트 수정 불필요
folder = content_map["pumpkin"]["folder"]  # 항상 "001_pumpkin"
```

---

## 마이그레이션 기록

### 2026-02-02 적용

```
변경 전:
- 001_pumpkin_published
- 024_beef_소고기_제작완료

변경 후:
- 001_pumpkin
- 024_beef_소고기
- 🔒_views/published/001_pumpkin -> ../../001_pumpkin
- 🔒_views/ready/024_beef_소고기 -> ../../024_beef_소고기
```

### 리네이밍 건수

| 유형 | 건수 |
|------|:----:|
| _published 제거 | 30 |
| _제작완료 제거 | 13 |
| **총계** | **43** |

---

## 참고

- **content_map 경로:** `config/data/content_map.json`
- **동기화 스크립트:** `services/scripts/sync_views.sh`
- **폴더 리네이밍 스크립트:** `services/scripts/folder_rename.sh`

---

**작성자:** 김부장 (자동화)
**검토:** PD님
